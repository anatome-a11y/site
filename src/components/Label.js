import React from 'react';


const Label = ({ children }) => <div style={{
    lineHeight: '1.2rem',
    marginBottom: 15,
    color: 'rgba(0, 0, 0, 0.85)',
}}>
    {children}
</div>

export default Label;